FROM nginx:1.24-alpine
WORKDIR /var/www/html

# Copy nginx site configuration
# Build context is backend/infra, so nginx/site.conf is relative to that
COPY nginx/site.conf /etc/nginx/conf.d/default.conf
