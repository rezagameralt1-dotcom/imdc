<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>@yield('title', 'DigitalCity')</title>
    @vite(['resources/css/app.css','resources/js/app.js'])
</head>
<body class="dc-body">
@include('partials.nav')

<main class="dc-container">
    @if (session('status'))
        <div class="dc-alert dc-alert-success">{{ session('status') }}</div>
    @endif

    @yield('content')
</main>
</body>
</html>

